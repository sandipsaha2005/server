export const addWeapons = (arg) => {
  return new Response("Hi", {
    headers: {
      "content-type": "text/html",
    },
  });
};
export const getWeapons = (arg) => {
  return new Response("Hi", {
    headers: {
      "content-type": "text/html",
    },
  });
};
