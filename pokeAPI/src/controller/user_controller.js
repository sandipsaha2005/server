export const getAllUsers = (arg) => {
  return new Response("Hi", {
    headers: {
      "content-type": "text/html",
    },
  });
};

export const addUser = (arg) => {
  return new Response("Hi", {
    headers: {
      "content-type": "text/html",
    },
  });
};

export const getUser = (arg) => {
  return new Response("Hi", {
    headers: {
      "content-type": "text/html",
    },
  });
};
